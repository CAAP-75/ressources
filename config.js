window.CAAP_CONFIG = {
  // Optionnel : collez ici l'URL /exec d'un Google Apps Script déployé en Web App.
  // La PWA fusionnera automatiquement les documents renvoyés par ce flux avec les ressources intégrées.
  REMOTE_FEED_URL: ""
};
